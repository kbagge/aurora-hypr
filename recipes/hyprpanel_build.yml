type: containerfile
snippets:
  - RUN git clone https://github.com/Jas-SinghFSU/HyprPanel.git /tmp/hyprpanel
  - RUN cd /tmp/hyprpanel
  - RUN meson setup build
  - RUN meson compile -C build
  - RUN meson install -C build